openapi: 3.1.0
info:
  title: ''
  version: '1.0.0'
paths:
  /ping:
    get:
      summary: Checks if the server is alive
      parameters:
        - in: header
          name: Request-ID
          schema:
            type: string
